#ENTRY#

Use to restore confirmed intended behavior.

Before Phase 2, establish the defect, intended behavior, full affected/intersecting boundary, likely repair scope, and minimum proof.

The plan must restore behavior without redesign, preserve unrelated behavior, cover direct dependencies/edge cases, and return any genuinely new product/workflow decision to planning.

Update durable KB prose only when the current contract changes or is wrong. Add permanent regression coverage only when approved or selected by policy.
