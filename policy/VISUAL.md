#ENTRY#

For a repository-dependent visual task, resolve only the relevant KB Tree
concept before inspecting the granted source or reference. Use rendered client
verification only when its `testing.client.rendered` contract makes it useful
and the task authorizes it.

Do not turn visual judgment into a broad desktop inspection. If the requested
work needs a product decision rather than a bounded visual treatment, return to
the planning route.
