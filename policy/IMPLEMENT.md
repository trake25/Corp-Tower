#ENTRY#

Use for approved work that creates or changes repository behavior/deliverables.

Planner must source-ground the Behavior, Retrieval, Changes, Invariants, and Verification sections. Do not copy universal Implementor rules into the plan.

Treat Planner-supplied feasibility/dependency traces, semantic interfaces, and cross-boundary invariants as part of the task contract when present.

Task-specific constraints belong in Invariants or Overrides only when they are not already universal policy.
