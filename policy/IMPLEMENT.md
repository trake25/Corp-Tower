#ENTRY#

Implement the authorized task contract.

Use the KB context loop from `policy/CODEX.md` only as needed while working.

Make only task-owned changes required to reach the approved intended behavior.

If implementation changes a durable current system contract, update its owning KB concept before close-out.

If the implementation introduces a genuinely new semantic responsibility with no existing KB owner, create the smallest concept that owns it.

Before editing authored KB prose, read and follow `KB/docs/context/CONCEPT-SCHEMA.md`.

KB prose describes the current system. Do not add task history, scratch notes, file or scene inventories, routine copied defaults, tunable values, or duplicated neighboring contracts.

Do not hand-edit generated KB routers or concept maps. Use the repository's deterministic tooling.

When implementation is complete, continue through the existing task-close review, verification, and close path.

Repair task-caused failures before closing.

Keep unrelated maintenance outside the task and hand it off through the repository's maintenance path when required.

After successful close-out, stop and report the implementation result.
