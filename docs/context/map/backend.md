# Map — Backend — `src/Server/**`

GENERATED by `scripts/build-file-map.mjs` — do not hand-edit line numbers.
The `Does` column IS hand-authored and is carried forward on regeneration.

**Grep this file, do not load it.** One hit costs ~150 tokens and gives you
file, line and purpose; loading the whole map costs thousands and gives you
the same one row you were looking for.

### src/Server/app/Bot_Manager.js — 559 ln

| Ln | Symbol | Does |
|---|---|---|
| 6 | BotManager · class | QA bot scheduler and placement policy; not production AI |
| 8 | startBots | start per-bot action loops |
| 38 | stopBots | stop every bot in the room |
| 60 | stopBot | stop one bot's timer; closed rooms must not keep ticking |
| 170 | hasClearedShareWhileTeammateShort | yield precondition; reads **banked** `score` |
| 207 | getVoidReleaseRows | gap-filling release-row candidates |
| 271 | rankByStrategy | cooperative vs mvp_greedy ordering; **never rank by `heightGain`** |
| 330 | chooseBotPlacement | column × release-row search, two-stage proxy then `evaluate()` |
| 478 | chooseBotAction | returns `{type:"place"}` or **`{type:"wait"}`**; callers must handle both |

### src/Server/app/Game_Config.js — 153 ln

| Ln | Symbol | Does |
|---|---|---|
| 1 | GameConfig · const | **every tuning value in one object** — read this file, not a prose copy |

### src/Server/app/Game_Engine.js — 1190 ln

| Ln | Symbol | Does |
|---|---|---|
| 8 | GameEngine · class | room facade; owns level lifecycle, timers, placement validation, Power |
| 19 | getRemainingMs | round clock remaining against `endsAt` |
| 39 | broadcastGameState | builds and sends the whole `game_state` payload clients render |
| 105 | createRoom | new room state, seat assignment, start level, first deal |
| 152 | hydrateRoom | rebuild room from a Redis snapshot on reconnect or pod handoff |
| 198 | restoreTimersFromState | restart level/tick timers after hydrate |
| 240 | persistRoom | notify Lobby Manager to snapshot; engine never writes Redis itself |
| 250 | queueQuickChat | quick chat cooldown check and event queue |
| 283 | consumeQuickChatEvents | drain pending quick chat for the next broadcast |
| 291 | consumePowerEvents | drain pending power-activation events for broadcast |
| 297 | clonePowerInventory | deep copy so snapshots don't alias live inventory |
| 301 | setupSideQuest | per-level shared side quest, gated by `powerUnlockLevel` |
| 307 | grantDefaultPowers | `powerGuaranteedBaseline` path, off by default |
| 318 | tryCompleteSideQuest | first exact finish claims the quest reward |
| 328 | activatePower | Power effects: replenish, refresh, score cap, copy score |
| 362 | getPostLevelTransitionDelayMs | popup duration + level summary delay |
| 369 | getPlacementScorePopupDurationMs | placement popup duration, clamped |
| 376 | getFinishScorePopupDurationMs | finish popup duration, clamped |
| 383 | getMaxScorePopupDurationMs | longest popup, sets the transition wait |
| 390 | startLevel | level init: target height, clock, quest, draw pile, opening hands |
| 431 | beginPlaying | `starting` → `playing`, arms the round clock |
| 452 | clearTimers | cancel start/level/tick timers |
| 464 | closeRoom | teardown, stop bots, clear timers |
| 485 | stopBots | delegate to Bot Manager |
| 495 | buildTargetHeightCurve | growing-step target height curve, cached |
| 525 | getTargetHeightForLevel | cached curve lookup per level |
| 546 | getLevelTimeLimitMs | derived round clock: slack ramp × cooldown, floored by `levelTimeLimitMs` |
| 582 | getConfiguredStartLevel | `debugStartLevel`, clamped |
| 586 | clampLevel | clamp to 1..`maxLevel` |
| 593 | restartAtConfiguredStartLevel | debug restart at the configured start level |
| 599 | restartAtLevel | restart every room at a level, optionally preserving score |
| 640 | getSupplyPackingEfficiency | cells per brick ÷ (height × effective width) |
| 660 | getSupplySiteWidthEstimate | site width estimate used for supply sizing |
| 675 | getSiteWidthForHeight | derives the even, viewport-clamped site from target height |
| 698 | getPlaceableColumnRange | broadcast `placeableColumnMin`/`Max` for the level |
| 715 | getPlaceableOriginRange | legal origin span for one brick's footprint |
| 727 | resolveColumnOriginX | clamps requested column into the placeable origin range |
| 744 | resolvePlacementOrigin | release-row legality and gap threading; `Number(null)` trap |
| 762 | placeBlock | authoritative placement: validate → settle → score → broadcast |
| 866 | getStabilityPressure | difficulty × level ramp → pressure scalar |
| 883 | resolveStabilityConfig | lerps stability anchors by pressure; every `evaluate()` caller sources here |
| 905 | recalculateTowerStability | re-runs Tower Stability over `entries` |
| 919 | checkWinCondition | target height reached, exact finish vs overbuild |
| 927 | checkFailCondition | impossibility test; uses the raw bound, never packing efficiency |
| 966 | anyPlayerCanRescueSupply | a held Replenish defers not-enough-height failure |
| 974 | completeLevel | bank scores, carry-over, summary, MVP, Impact gate |
| 1041 | failLevel | level failure, discard carry-over, Impact rollback |
| 1085 | nextLevel | advance level or finish the match at `maxLevel` |

### src/Server/app/Lobby_Manager.js — 1192 ln

| Ln | Symbol | Does |
|---|---|---|
| 6 | DEFAULT_DEBUG_CONFIG · const | startup snapshot every debug reset restores to |
| 60 | LobbyManager · class | matchmaking, room lifecycle, reconnect, debug-config coordinator |
| 71 | start | boot: Redis wiring, assignment subscription |
| 86 | handlePlayerAssignment | cross-pod handoff receiver; adopts a player assigned elsewhere |
| 100 | createPlayer | socket → player, applies a `reconnect` request |
| 149 | addPlayer | enqueue for matchmaking; enqueues *before* taking the lock |
| 159 | resumePlayer | rejoin an existing room after disconnect |
| 220 | removePlayer | disconnect handling, reconnect grace window |
| 249 | leaveQueue | explicit dequeue |
| 258 | scheduleRoomReconnectExpiry | arm the room-abandon timer |
| 278 | cancelRoomReconnectExpiry | disarm on successful rejoin |
| 289 | handleRoomReconnectExpired | close a room nobody came back to |
| 314 | resetParticipantState | clear per-room state off a player |
| 325 | isConnectedRealPlayer | excludes bots and dead sockets |
| 333 | sendPlayer | single-socket send guard |
| 339 | closeRoom | room teardown, roster notify, Redis cleanup |
| 395 | resetBotCounterIfIdle | bot naming counter reset |
| 407 | getRealPlayers | non-bot connected players |
| 433 | broadcastDebugConfig | push `debug_config` to every client |
| 444 | getDebugConfig | current debug-tunable snapshot for the wire |
| 506 | applyDefaultDebugConfig | write `DEFAULT_DEBUG_CONFIG` back into `GameConfig` |
| 594 | resetDebugConfigToDefaults | restore every exposed tunable, then rebroadcast |
| 621 | updateDebugConfig | **the authoritative clamp**: every debug key's range and allowlist |
| 814 | restartRoomsAtDebugStartLevel | apply `debugStartLevel` to live rooms |
| 828 | restartRoomsAtCurrentLevel | `restartLevel` action; preserves total score |
| 844 | createBot | debug bot participant |
| 853 | fillQueueWithBotsIfNeeded | fill seats only when a real player waits |
| 875 | removeWaitingBots | drop queued bots |
| 881 | syncWaitingBots | reconcile queued bot count to `debugBotCount` |
| 901 | refreshMatchmaking | re-run matching after queue change |
| 906 | tryCreateRoom | atomic `dequeueRealPlayers(3)` then room creation |
| 942 | createRoom | 3-seat room, engine start, Redis publish |
| 1005 | buildRoomRoster | roster payload with profiles and colours |
| 1017 | createEngine | construct Game Engine with persist/broadcast callbacks |
| 1036 | hydrateRoom | rebuild a room this pod did not create |
| 1102 | subscribeRoom | listen on `room:<id>:actions` |
| 1138 | isRoomOwner | lease check; **removing it makes every pod write divergent state** |
| 1147 | dispatchRoomAction | run locally if owner, else republish to the lease owner |
| 1165 | runRoomAction | execute a room action against the live engine |

### src/Server/app/Profile_Store.js — 53 ln

| Ln | Symbol | Does |
|---|---|---|
| 1 | WORD_LIST · const | word pool for generated display names |
| 6 | hashString · fn | stable hash → deterministic name per player id |
| 14 | ProfileStore · class | display-name/profile lookup |
| 19 | connect | attach to Redis, or fall back to in-memory |
| 22 | getProfile | profile for a player id, generating one if absent |

### src/Server/app/Redis_State.js — 547 ln

| Ln | Symbol | Does |
|---|---|---|
| 3 | RECONNECT_TTL_SECONDS · const | how long a disconnected session stays resumable |
| 6 | ROOM_LEASE_SECONDS · const | room ownership lease TTL |
| 9 | POD_ID · const | this worker's identity, used for lease ownership |
| 14 | REDIS_CONNECT_RETRIES · const | connect retry count |
| 17 | REDIS_CONNECT_RETRY_DELAY_MS · const | connect retry backoff |
| 20 | positiveNumberFromEnv · fn | env override parsing with a floor |
| 26 | sleep · fn | retry delay helper |
| 30 | stripRuntimePlayer · fn | drop sockets/timers before a player is serialised |
| 50 | DRAW_PILE_SNAPSHOT_LIMIT · const | cap on pile entries written to a snapshot |
| 52 | stripRuntimeRoom · fn | drop non-serialisable room state before persist |
| 91 | RedisState · class | shared-state adapter; in-memory fallback when no `REDIS_URL` |
| 107 | connect | connect or fall back to in-memory maps |
| 136 | connectRedisClients | main + subscriber clients with retry |
| 188 | closeRedisClient | teardown |
| 204 | getPodId | this pod's id |
| 208 | getReconnectTtlSeconds | resolved reconnect TTL |
| 212 | nextPlayerId | id allocation |
| 221 | nextRoomId | id allocation |
| 229 | createReconnectToken | token a client presents to resume |
| 233 | saveSession | persist player session for reconnect |
| 262 | getSession | look up a session by reconnect token |
| 275 | markSessionDisconnected | start the reconnect grace window |
| 293 | enqueuePlayer | matchmaking queue push |
| 306 | removeQueuedPlayer | queue removal on leave/disconnect |
| 320 | getQueuedPlayers | queue read |
| 329 | dequeueRealPlayers | **atomic** 3-player drain; the non-atomic path loses cross-pod entries |
| 345 | requeuePlayers | return players to the queue on failed room creation |
| 367 | withMatchmakingLock | cross-pod matchmaking mutex |
| 393 | saveRoom | room snapshot write |
| 424 | claimRoomLease | take/renew room ownership; basis of `isRoomOwner` |
| 437 | getRoomLeaseOwner | which pod owns this room |
| 445 | getRoom | room snapshot read |
| 458 | deleteRoom | snapshot + lease cleanup |
| 472 | publishRoom | broadcast room state to other pods |
| 486 | subscribeToRoom | receive other pods' room broadcasts |
| 496 | publishRoomAction | send an action to the lease-owning pod |
| 510 | subscribeToRoomActions | receive actions for rooms this pod owns |
| 520 | publishPlayerAssignment | hand a player to another pod |
| 531 | subscribeToPlayerAssignments | receive handed-off players |

### src/Server/app/Server.js — 118 ln

| Ln | Symbol | Does |
|---|---|---|
| 5 | lobbyManager · const | singleton instance |
| 7 | port · const | listen port from env |
| 9 | safeJson · fn | parse guard; malformed frames never throw |
| 18 | main · fn | WebSocket entry point, connection accept, first `reconnect` |
| 59 | handleMessage · fn | **the message router** — every client message type dispatches here |

### src/Server/app/Tower_Stability.js — 292 ln

| Ln | Symbol | Does |
|---|---|---|
| 3 | cellsFor · fn | absolute grid cells for one placed entry |
| 12 | key · fn | `x,y` cell key for occupancy sets |
| 14 | clamp01 · fn | clamp helper used by every penalty term |
| 16 | topHeight · fn | current tower height from entries |
| 20 | cellsForEntries · fn | occupancy set across the whole tower |
| 26 | settleBlock · fn | **gravity settle**; `SnapGrid.settle_origin_y` mirrors this line for line |
| 39 | blockCells · fn | cells of a block at an origin |
| 50 | isPlacementLegal · fn | overlap / below-platform / site bounds; **`SnapGrid.is_placement_legal` mirrors it** |
| 70 | supportedCellsGained · fn | cells put back on solid ground; what Reinforce pays for |
| 94 | evaluate · fn | **the pure stability function** — lean, integrity, slenderness, support deficit, collapse |
| 257 | structuralLean · fn | CoM offset + column imbalance + new-brick overhang |
| 277 | balanceDelta · fn | per-placement lean movement; drives brick mood faces, **lean only** |

### src/Server/app/engine/Block_Supply.js — 653 ln

| Ln | Symbol | Does |
|---|---|---|
| 3 | getNextDrawBlock · fn | shared "Next Draw" preview head |
| 13 | createBlockId · fn | unique block id |
| 17 | cloneCells · fn | copy cells so orientations never alias |
| 21 | getBlockHeight · fn | vertical footprint at this orientation, not cell count |
| 42 | getBlockCellCount · fn | occupied cell count |
| 54 | pickWeightedShape · fn | weighted draw across the 5 brick shapes |
| 87 | normalizeCells · fn | shift cells to origin after transform |
| 93 | rotateCellsCW · fn | 90° rotation; `BlockData` mirrors this maths |
| 97 | reflectCellsX · fn | mirror; client mirrors via UV winding instead |
| 101 | getRotations · fn | distinct rotations of a shape |
| 130 | getOrientations · fn | the 19 reachable rotate+mirror orientations |
| 155 | createBlock · fn | block with random orientation at generation |
| 181 | getRandomBlock · fn | one weighted random block |
| 185 | getBlocksPerPlayer · fn | hand size; refill targets this |
| 197 | buildDrawPile · fn | carry-over + generated reserve, shuffled |
| 214 | getAverageBrickHeight · fn | drives supply sizing and the reinforce cap |
| 233 | getAverageBrickCellCount · fn | numerator of packing efficiency |
| 252 | getLevelSupplyMaxSurplus · fn | flat amount **plus** a share of the requirement |
| 266 | getSupplyCoverageShare · fn | coverage lerp start→end by `levelSupplyCoverageFullLevel` |
| 276 | getSuppliedBrickHeight · fn | `ceil(targetHeight / efficiency)` × coverage |
| 280 | getGeneratedDrawPileBlockCount · fn | reserve size; `maxGeneratedDrawPileBlocks` is a sanity ceiling only |
| 310 | generateDrawPileBlocks · fn | fill the reserve |
| 320 | generateSolvableOpeningHandBlocks · fn | retry until the opening hand can reach target |
| 357 | isLevelBlockSupplyValid · fn | solvability check over a candidate supply |
| 381 | getTotalBlockHeight · fn | summed vertical footprint of a block set |
| 387 | countPrecisionBlocks · fn | blocks of height ≤ 2, for `minPrecisionBlocksPerLevel` |
| 399 | hasExactHeightCombination · fn | subset-sum for an exact finish; **returns on first hit**, O(blocks × targetHeight) |
| 429 | shuffleBlocks · fn | pile shuffle |
| 440 | dealOpeningHands · fn | opening hands bypass the pile; calls `trimInventory` via the facade |
| 457 | drawBlockFromPile · fn | pop the shared pile |
| 465 | refillPlayerBlock · fn | top a hand to `getBlocksPerPlayer` from the pile |
| 477 | trimInventory · fn | cap a hand at `maxActiveBlocks` |
| 498 | generateRefreshBlocks · fn | Refresh power: reroll a hand toward remaining height |
| 529 | getReplenishBlockCount · fn | `powerReplenishPileShare` × `drawPileStartCount`, min 1 |
| 541 | generateReplenishBlocks · fn | append to the pile; caller then refills every hand |
| 555 | createRefreshBlock · fn | one replacement block, avoiding the current shape |
| 562 | isRefreshBlockSetUseful · fn | `refreshMinUsefulBlockHeight` gate on a reroll |
| 578 | scoreRefreshBlockSet · fn | rank candidate reroll sets |
| 593 | prepareTeamCarryOverBlocks · fn | carry-over on completion, precision first; **discarded on failure** |

### src/Server/app/engine/Impacts.js — 328 ln

| Ln | Symbol | Does |
|---|---|---|
| 3 | saveImpactScores · fn | snapshot leaderboard scores at an Impact |
| 15 | saveImpactPowers · fn | snapshot Power inventories (`powerLifetime: impact`) |
| 28 | saveImpactState · fn | both snapshots together |
| 33 | ensureImpactScores · fn | lazily create the score snapshot |
| 42 | ensureImpactPowers · fn | lazily create the power snapshot |
| 51 | ensureImpactState · fn | lazily create both |
| 56 | restoreImpactScores · fn | roll scores back to the last passed Impact |
| 64 | restoreImpactPowers · fn | roll inventories back; stops farming by repeated failure |
| 78 | awardImpactPower · fn | Impact-MVP grant path, off by default |
| 107 | isImpactLevel · fn | is this level an Impact boundary (`impactInterval`) |
| 113 | getImpactScoreRequirement · fn | optional flat floor |
| 117 | getImpactMinContributionShare · fn | per-player share of the pool |
| 133 | getExpectedPlacementScoreForLevel · fn | expected pay per level, incl. the 0.85 stability discount |
| 144 | getExpectedPlacementScoreForImpactBand · fn | summed across the band |
| 160 | getImpactBandScoreRequirement · fn | **the gate value** each player must reach |
| 173 | getImpactScoreFailures · fn | who missed the gate |
| 178 | getNextImpactLevel · fn | next Impact boundary |
| 189 | getImpactScoreStatus · fn | per-player standing; reads **banked** `score`, so callers add live `levelScore` |
| 223 | hasMetImpactScoreRequirement · fn | single-player gate check; simulator routes here |
| 227 | failImpactScoreRequirement · fn | gate failure → rollback |
| 287 | rollbackToImpact · fn | restore scores and powers to the last passed Impact |

### src/Server/app/engine/Scoring.js — 383 ln

| Ln | Symbol | Does |
|---|---|---|
| 3 | createScoreEvent · fn | **start here for a new score event type** — builds the wire shape |
| 22 | queueScoreEvent · fn | queue a score event for the next broadcast |
| 34 | consumeScoreEvents · fn | drain queued events into `game_state` |
| 45 | getPlayerScoreMap · fn | per-player score lookup |
| 55 | getTeamLevelScore · fn | summed level score |
| 61 | getPlayerBonusBreakdown · fn | bonus components per player |
| 74 | buildLevelSummary · fn | end-of-level summary payload, MVP, breakdowns |
| 117 | recordScoreBreakdown · fn | accumulate per-category score for the summary |
| 127 | getPlacementStabilityMultiplier · fn | `placementStabilityFloor` lerp on **inherited** stability |
| 149 | addPlacementScore · fn | the core earner; height × level × multiplier |
| 194 | getReinforceScoreCap · fn | repair ceiling from `avgBrickHeight`; re-prices with the brick mix |
| 217 | addReinforceScore · fn | integrity gain, lean correction, supported cells |
| 264 | awardCompletionBonuses · fn | precision and team-exact bonuses |
| 303 | addBonusScore · fn | apply one bonus and emit its event |
| 322 | getBonusScoreEventType · fn | bonus → wire event type |
| 333 | getBonusScoreEventLabel · fn | bonus → display label |
| 344 | addLevelScoreToLeaderboard · fn | **the only banking step**; failed levels never bank |
| 351 | getLevelMVP · fn | highest level score; display only |

### src/Server/tools/Balance_Simulator.js — 631 ln

| Ln | Symbol | Does |
|---|---|---|
| 6 | STRATEGIES · const | both bot strategies are run per level for comparison |
| 8 | DEFAULT_LEVELS · const | levels sampled per run |
| 12 | DEFAULT_RUNS · const | runs per level; 100 balances runtime against rate precision |
| 17 | SWEEP_DIFFICULTIES · const | stability sweep points |
| 18 | SWEEP_LEVEL_STEP · const | sweep level stride |
| 20 | createPlayers · fn | synthetic 3-seat roster |
| 28 | createEngineForLevel · fn | constructs Game Engine directly — no lobby, Redis or socket |
| 46 | withMutedConsole · fn | silence engine logging during sampling |
| 63 | nextPlayerToAct · fn | **the cooldown clock**: earliest-ready player; omitting it fakes the gate rate |
| 83 | chooseSmartPlacement · fn | delegates to the shipped Bot Manager, not a parallel heuristic |
| 112 | simulateSmartPlay · fn | one full level simulation |
| 287 | getScoreSummary · fn | per-run score aggregation |
| 313 | meetsImpactGate · fn | routes through the engine's real gate check |
| 317 | runLevel · fn | sample one level across both strategies |
| 455 | percent · fn | rate formatting |
| 459 | printResults · fn | completion, gate, MVP table |
| 533 | printStabilityResults · fn | `avgStability`, `integrityBinding`, per-placement spread |
| 579 | runSweep · fn | `balance:stability` difficulty sweep |
| 600 | main · fn | CLI entry |

### src/Server/tools/Stability_Probe.js — 252 ln

| Ln | Symbol | Does |
|---|---|---|
| 19 | LEVELS · const | levels probed |
| 20 | HEIGHT_FRACTIONS · const | tower heights sampled per archetype |
| 22 | createPlayers · fn | synthetic roster |
| 30 | withMutedConsole · fn | silence engine logging |
| 42 | createEngineForLevel · fn | engine without lobby, Redis or socket |
| 59 | rowEntry · fn | build one placed-brick row |
| 69 | centeredOriginX · fn | centre a shape on the site |
| 74 | ARCHETYPES · const | hand-built towers: spire, wide base, overhang — deterministic, unlike bot play |
| 159 | leanScore · fn | lean readout for an archetype |
| 169 | run · fn | probe entry; prints stability per archetype × height |
| 216 | assertOpeningBrickSurvives · fn | **regression guard**: one narrow opening brick must not collapse at any level |

---

13 files · 266 symbols · 0 awaiting a `Does` line.
