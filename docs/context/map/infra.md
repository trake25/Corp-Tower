# Map — Infra — `infra/` · `.github/` · `scripts/`

GENERATED by `scripts/build-file-map.mjs` — do not hand-edit line numbers.
The `Does` column IS hand-authored and is carried forward on regeneration.

**Grep this file, do not load it.** One hit is self-sufficient — it carries
`path:line` and purpose, so it feeds straight into a `Read` with no second
lookup. Loading the whole map costs thousands and gives you the same one row.

### .github/actions/aws-terraform-setup/action.yml — 30 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/actions/aws-terraform-setup/action.yml:6 | inputs · key | `role-to-assume`, `aws-region`, and `setup-terraform` (default true) |
| .github/actions/aws-terraform-setup/action.yml:18 | runs · key | **the shared AWS entry point** — OIDC credential assumption via configure-aws-credentials@v6, then optional Terraform CLI install; every workflow that talks to AWS goes through it |

### .github/actions/build-godot-web/action.yml — 125 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/actions/build-godot-web/action.yml:8 | inputs · key | `godot-version`, `godot-status`, `project-path`, `output-dir`, `preset-config` |
| .github/actions/build-godot-web/action.yml:25 | outputs · key | `largest-file-bytes` and `largest-file-name`, so a caller can gate on export size |
| .github/actions/build-godot-web/action.yml:33 | runs · key | installs Godot and export templates, copies the CI preset in, imports headless, exports HTML5, and **fails if the export lacks index.html, a .wasm or a .pck**; assumes fetch-private-assets already ran |

### .github/actions/eks-infra-drift-check/action.yml — 40 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/actions/eks-infra-drift-check/action.yml:8 | inputs · key | `aws-region`, which locates the shared state bucket |
| .github/actions/eks-infra-drift-check/action.yml:13 | runs · key | **deploy workflows run no Terraform, so a pushed infra change is inert until EKS Infra Apply runs**; compares the checked-out tree against the S3 marker and fails on mismatch, warning only when no marker exists |

### .github/actions/fetch-private-assets/action.yml — 175 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/actions/fetch-private-assets/action.yml:9 | inputs · key | manifest path, dest path, and the four R2 credentials; **`version-override` skips sha256 verification and must never be used for a release build** |
| .github/actions/fetch-private-assets/action.yml:36 | runs · key | **fails closed** — verifies sha256, file count and sentinel files, and redacts key-shaped strings from aws errors. `AWS_REGION` is pinned to `auto` or a caller's region leaks in and R2 rejects it |

### .github/actions/terraform-backend-bootstrap/action.yml — 42 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/actions/terraform-backend-bootstrap/action.yml:6 | inputs · key | `aws-region`, which names the bucket `corp-tower-tfstate-<region>` |
| .github/actions/terraform-backend-bootstrap/action.yml:11 | runs · key | **idempotent**: creates the shared state bucket if absent, then always re-asserts versioning, AES256 encryption and full public-access block |

### .github/actions/terraform-validate-plan/action.yml — 39 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/actions/terraform-validate-plan/action.yml:7 | inputs · key | `working-directory`, the stack directory to run in |
| .github/actions/terraform-validate-plan/action.yml:12 | runs · key | the shared check sequence — init, **`fmt -check`**, validate, plan saved to `./tfplan` for a later apply step, then a readable plan summary |

### .github/workflows/Android-Deploy-wstodplay.yml — 419 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/Android-Deploy-wstodplay.yml:3 | on · key | manual only, with inputs |
| .github/workflows/Android-Deploy-wstodplay.yml:31 | jobs · key | single job: signed AAB build and Google Play upload |
| .github/workflows/Android-Deploy-wstodplay.yml:32 | build-android · job | fetches private art, writes the client endpoint config pinned to the EKS `wstodplay` host, resolves the next Play version code via the Developer API, then builds and uploads a signed AAB on Java 17 |

### .github/workflows/Backup-Cleanup-All.yml — 101 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/Backup-Cleanup-All.yml:9 | on · key | manual only, with a confirmation input and target/instance selectors |
| .github/workflows/Backup-Cleanup-All.yml:35 | jobs · key | a confirmation guard, then four independent per-instance teardowns |
| .github/workflows/Backup-Cleanup-All.yml:36 | guard · job | **typed confirmation must match the target or nothing runs** — every cleanup job needs it |
| .github/workflows/Backup-Cleanup-All.yml:70 | cleanup-devwstod1 · job | game instance 1, skipped when target is Web only or instance is 2 |
| .github/workflows/Backup-Cleanup-All.yml:78 | cleanup-devwstod2 · job | game instance 2, skipped when target is Web only or instance is 1 |
| .github/workflows/Backup-Cleanup-All.yml:86 | cleanup-devtod1 · job | web instance 1, skipped when target is Game only or instance is 2 |
| .github/workflows/Backup-Cleanup-All.yml:94 | cleanup-devtod2 · job | web instance 2, skipped when target is Game only or instance is 1 |

### .github/workflows/Backup-Cleanup-Game-Server.yml — 40 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/Backup-Cleanup-Game-Server.yml:3 | on · key | `workflow_call` only — a reusable core, never dispatched directly |
| .github/workflows/Backup-Cleanup-Game-Server.yml:15 | jobs · key | one job on the self-hosted backup runner |
| .github/workflows/Backup-Cleanup-Game-Server.yml:16 | cleanup-backup · job | resolves the instance number and runs the backup server down script on the physical machine |

### .github/workflows/Backup-Cleanup-Web-Server.yml — 40 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/Backup-Cleanup-Web-Server.yml:3 | on · key | `workflow_call` only |
| .github/workflows/Backup-Cleanup-Web-Server.yml:15 | jobs · key | one job on the self-hosted backup runner |
| .github/workflows/Backup-Cleanup-Web-Server.yml:16 | cleanup-backup · job | stands down the HTML5 web instance on the physical backup machine |

### .github/workflows/Backup-Deploy-All.yml — 230 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/Backup-Deploy-All.yml:15 | on · key | **push to the tracked branch on matching paths, plus manual** — the only backup path that deploys automatically |
| .github/workflows/Backup-Deploy-All.yml:55 | jobs · key | resolve the plan, check what is already running, then deploy game and web instances in dependency order |
| .github/workflows/Backup-Deploy-All.yml:56 | resolve · job | computes `deploy_game`/`deploy_web` outputs that every downstream job gates on |
| .github/workflows/Backup-Deploy-All.yml:139 | check-devwstod1-status · job | **push-triggered only** — looks for an already-running container so a push does not redeploy an idle instance |
| .github/workflows/Backup-Deploy-All.yml:159 | check-devtod1-status · job | same running-container check for the web side |
| .github/workflows/Backup-Deploy-All.yml:179 | deploy-devwstod1 · job | game instance 1; gated on the resolve plan and the status check |
| .github/workflows/Backup-Deploy-All.yml:191 | deploy-devwstod2 · job | game instance 2, **serialised behind instance 1** rather than run in parallel |
| .github/workflows/Backup-Deploy-All.yml:203 | deploy-devtod1 · job | web instance 1; gated on the resolve plan and the status check |
| .github/workflows/Backup-Deploy-All.yml:217 | deploy-devtod2 · job | web instance 2, serialised behind web instance 1 |

### .github/workflows/Backup-Deploy-Game-Server.yml — 42 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/Backup-Deploy-Game-Server.yml:3 | on · key | `workflow_call` only |
| .github/workflows/Backup-Deploy-Game-Server.yml:15 | jobs · key | one job on the self-hosted backup runner |
| .github/workflows/Backup-Deploy-Game-Server.yml:16 | deploy-backup · job | resolves the instance number and runs the backup server up script, which also self-records the deployed SHA |

### .github/workflows/Backup-Deploy-Web-Server.yml — 135 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/Backup-Deploy-Web-Server.yml:3 | on · key | `workflow_call` only |
| .github/workflows/Backup-Deploy-Web-Server.yml:20 | jobs · key | resolve target, build the web export on a GitHub runner, then hand the artifact to the backup machine |
| .github/workflows/Backup-Deploy-Web-Server.yml:21 | resolve-target · job | picks which web instance this run targets |
| .github/workflows/Backup-Deploy-Web-Server.yml:63 | build · job | fetches private art, writes the endpoint config, builds the web export, uploads it, and **removes the private assets from the runner** |
| .github/workflows/Backup-Deploy-Web-Server.yml:115 | deploy-to-backup · job | downloads the artifact on the self-hosted runner and deploys it locally |

### .github/workflows/Backup-Diagnose.yml — 42 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/Backup-Diagnose.yml:3 | on · key | manual only |
| .github/workflows/Backup-Diagnose.yml:6 | jobs · key | one read-only job on the backup machine |
| .github/workflows/Backup-Diagnose.yml:7 | diagnose · job | reports status for every backup instance — game devwstod1/2, web devtod1/2, and the wstoddemo container |

### .github/workflows/Demo-Cleanup.yml — 68 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/Demo-Cleanup.yml:8 | on · key | manual only, with a confirmation input |
| .github/workflows/Demo-Cleanup.yml:25 | jobs · key | confirmation guard, then the two demo teardowns |
| .github/workflows/Demo-Cleanup.yml:26 | guard · job | typed confirmation must match the target before either cleanup runs |
| .github/workflows/Demo-Cleanup.yml:53 | cleanup-wstoddemo · job | tears down the demo game instance |
| .github/workflows/Demo-Cleanup.yml:61 | cleanup-toddemo · job | tears down the demo web instance |

### .github/workflows/Demo-Deploy.yml — 47 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/Demo-Deploy.yml:9 | on · key | manual only, with a target selector |
| .github/workflows/Demo-Deploy.yml:27 | jobs · key | game then web, serialised |
| .github/workflows/Demo-Deploy.yml:28 | deploy-wstoddemo · job | demo game instance; runs unless the target is Web only |
| .github/workflows/Demo-Deploy.yml:35 | deploy-toddemo · job | demo web instance, **serialised behind the game deploy** so the client has a server to point at |

### .github/workflows/EKS-Cleanup-All.yml — 36 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/EKS-Cleanup-All.yml:3 | on · key | manual only, with a target selector |
| .github/workflows/EKS-Cleanup-All.yml:20 | jobs · key | fan-out to the two reusable cleanup cores |
| .github/workflows/EKS-Cleanup-All.yml:21 | cleanup-game · job | calls the game cleanup core for `wstodplay` |
| .github/workflows/EKS-Cleanup-All.yml:29 | cleanup-web · job | calls the web cleanup core for `todplay` |

### .github/workflows/EKS-Cleanup-Game-Server.yml — 64 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/EKS-Cleanup-Game-Server.yml:7 | on · key | `workflow_call` only |
| .github/workflows/EKS-Cleanup-Game-Server.yml:27 | jobs · key | one job in the `staging` environment |
| .github/workflows/EKS-Cleanup-Game-Server.yml:28 | cleanup-game · job | OIDC into AWS, updates kubeconfig, and **deletes the game server Kubernetes resources** — the cluster itself is untouched |

### .github/workflows/EKS-Cleanup-Web-Server.yml — 72 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/EKS-Cleanup-Web-Server.yml:9 | on · key | `workflow_call` only |
| .github/workflows/EKS-Cleanup-Web-Server.yml:29 | jobs · key | one job in the `staging` environment |
| .github/workflows/EKS-Cleanup-Web-Server.yml:30 | cleanup-web · job | **swaps the web deployment to a maintenance placeholder rather than deleting it**, so the hostname keeps serving something |

### .github/workflows/EKS-Deploy-All.yml — 36 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/EKS-Deploy-All.yml:3 | on · key | manual only, with a target selector |
| .github/workflows/EKS-Deploy-All.yml:20 | jobs · key | fan-out to the two reusable deploy cores |
| .github/workflows/EKS-Deploy-All.yml:21 | deploy-game · job | calls the game deploy core for `wstodplay` |
| .github/workflows/EKS-Deploy-All.yml:29 | deploy-web · job | calls the web deploy core for `todplay` |

### .github/workflows/EKS-Deploy-Game-Server.yml — 465 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/EKS-Deploy-Game-Server.yml:3 | on · key | `workflow_call` only |
| .github/workflows/EKS-Deploy-Game-Server.yml:23 | jobs · key | test and infra-verify in parallel, then build/push, then deploy — **every stage gates the next on `result == 'success'`** |
| .github/workflows/EKS-Deploy-Game-Server.yml:24 | test-server · job | runs the server test suite on a GitHub VM before anything is built |
| .github/workflows/EKS-Deploy-Game-Server.yml:49 | verify-infra · job | **fails fast when the tree has infra changes that were never applied**, so a deploy cannot target a stale cluster |
| .github/workflows/EKS-Deploy-Game-Server.yml:71 | build-and-push · job | OIDC to ECR, Buildx build and push, and records the pushed image tag for the deploy job |
| .github/workflows/EKS-Deploy-Game-Server.yml:117 | deploy-eks · job | reads Terraform outputs, updates kubeconfig, applies the manifests, and **ends in a WebSocket smoke test** rather than trusting rollout status |

### .github/workflows/EKS-Deploy-Web-Server.yml — 349 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/EKS-Deploy-Web-Server.yml:3 | on · key | `workflow_call` only |
| .github/workflows/EKS-Deploy-Web-Server.yml:26 | jobs · key | resolve, verify infra, build the export, push the image, deploy |
| .github/workflows/EKS-Deploy-Web-Server.yml:27 | resolve-target · job | picks the target environment for the rest of the run |
| .github/workflows/EKS-Deploy-Web-Server.yml:53 | verify-infra · job | same unapplied-infra fast fail as the game core |
| .github/workflows/EKS-Deploy-Web-Server.yml:75 | build · job | Godot web export with private art, then **removes the private assets from the runner** before the job ends |
| .github/workflows/EKS-Deploy-Web-Server.yml:122 | push · job | wraps the web build in a Docker image and pushes it to ECR |
| .github/workflows/EKS-Deploy-Web-Server.yml:160 | deploy-eks · job | deploys to the cluster and **updates the Cloudflare CNAME to the current ALB DNS name** — a fresh apply changes that value every session |

### .github/workflows/EKS-Force-Unlock.yml — 59 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/EKS-Force-Unlock.yml:8 | on · key | manual only, with a confirmation input |
| .github/workflows/EKS-Force-Unlock.yml:27 | jobs · key | one guarded job |
| .github/workflows/EKS-Force-Unlock.yml:28 | force-unlock · job | **breaks a stuck S3 state lock by ID** after a typed confirmation; the recovery path when a run dies mid-apply |

### .github/workflows/EKS-Infra-Apply.yml — 72 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/EKS-Infra-Apply.yml:3 | on · key | manual only, with a confirmation input |
| .github/workflows/EKS-Infra-Apply.yml:22 | jobs · key | one guarded job in the `staging` environment |
| .github/workflows/EKS-Infra-Apply.yml:23 | apply · job | confirmation, backend bucket bootstrap, validate and plan, **then apply — the plan gate runs inside the same job** |

### .github/workflows/EKS-Infra-Auto-Destroy.yml — 90 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/EKS-Infra-Auto-Destroy.yml:7 | on · key | **scheduled `0 18 * * *` plus manual** — the nightly cost guard |
| .github/workflows/EKS-Infra-Auto-Destroy.yml:23 | jobs · key | one job that no-ops when no session stack exists |
| .github/workflows/EKS-Infra-Auto-Destroy.yml:24 | auto-destroy · job | **destroys the EKS session stack every night at 18:00 UTC**; checks first that a stack exists, and never touches the persistent `eks-shared` root |

### .github/workflows/EKS-Infra-Destroy.yml — 123 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/EKS-Infra-Destroy.yml:3 | on · key | manual only, with a confirmation input |
| .github/workflows/EKS-Infra-Destroy.yml:22 | jobs · key | one guarded job |
| .github/workflows/EKS-Infra-Destroy.yml:23 | destroy · job | typed confirmation, then `terraform destroy` of the session stack |

### .github/workflows/EKS-Infra-Diagnose.yml — 119 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/EKS-Infra-Diagnose.yml:3 | on · key | manual or `workflow_call` |
| .github/workflows/EKS-Infra-Diagnose.yml:15 | jobs · key | one read-only job |
| .github/workflows/EKS-Infra-Diagnose.yml:16 | diagnostics · job | reads Terraform outputs, then shows node status and **ALB target group health**, which is where a failed deploy usually shows first |

### .github/workflows/EKS-Infra-Plan.yml — 44 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/EKS-Infra-Plan.yml:3 | on · key | manual or `workflow_call` |
| .github/workflows/EKS-Infra-Plan.yml:18 | jobs · key | one read-only job |
| .github/workflows/EKS-Infra-Plan.yml:19 | plan · job | ensures the backend bucket exists, then validates and plans without applying |

### .github/workflows/EKS-Shared-Infra-Apply.yml — 69 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/EKS-Shared-Infra-Apply.yml:9 | on · key | manual only, with a confirmation input |
| .github/workflows/EKS-Shared-Infra-Apply.yml:27 | jobs · key | one guarded job |
| .github/workflows/EKS-Shared-Infra-Apply.yml:28 | apply · job | applies the **persistent** ACM/Cloudflare root in `eks-shared` state; it must outlive session stacks, so it has no destroy counterpart |

### .github/workflows/Site-Cleanup-Workers.yml — 54 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/Site-Cleanup-Workers.yml:10 | on · key | manual only |
| .github/workflows/Site-Cleanup-Workers.yml:17 | jobs · key | one job |
| .github/workflows/Site-Cleanup-Workers.yml:18 | deploy-maintenance · job | generates a maintenance placeholder into `site/dist` and deploys it over the live Worker |

### .github/workflows/Site-Deploy-Workers.yml — 58 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/Site-Deploy-Workers.yml:10 | on · key | **push on `site/` paths plus manual** — the portfolio site deploys itself |
| .github/workflows/Site-Deploy-Workers.yml:24 | jobs · key | one build-and-deploy job |
| .github/workflows/Site-Deploy-Workers.yml:25 | build-and-deploy · job | builds the Astro site and deploys to Cloudflare Workers; **no game code is involved** |

### .github/workflows/Site-Root-Deploy-Workers.yml — 46 ln

| File:Ln | Symbol | Does |
|---|---|---|
| .github/workflows/Site-Root-Deploy-Workers.yml:10 | on · key | push on `site-root/` paths plus manual |
| .github/workflows/Site-Root-Deploy-Workers.yml:24 | jobs · key | one job |
| .github/workflows/Site-Root-Deploy-Workers.yml:25 | deploy · job | deploys the apex-domain placeholder Worker |

### infra/eks/apps/corp-tower/base/kustomization.yaml — 4 ln

_no extracted symbols_

### infra/eks/apps/corp-tower/base/server-deployment.yaml — 72 ln

_no extracted symbols_

### infra/eks/apps/corp-tower/base/server-service.yaml — 19 ln

_no extracted symbols_

### infra/eks/apps/corp-tower/overlays/eks-prod/game/kustomization.yaml — 11 ln

_no extracted symbols_

### infra/eks/apps/corp-tower/overlays/eks-prod/game/namespace.yaml — 9 ln

_no extracted symbols_

### infra/eks/apps/corp-tower/overlays/eks-prod/web-maintenance/kustomization.yaml — 6 ln

_no extracted symbols_

### infra/eks/apps/corp-tower/overlays/eks-prod/web-maintenance/namespace.yaml — 9 ln

_no extracted symbols_

### infra/eks/apps/corp-tower/overlays/eks-prod/web/kustomization.yaml — 11 ln

_no extracted symbols_

### infra/eks/apps/corp-tower/overlays/eks-prod/web/namespace.yaml — 9 ln

_no extracted symbols_

### infra/eks/apps/corp-tower/overlays/eks-test/game/kustomization.yaml — 20 ln

_no extracted symbols_

### infra/eks/apps/corp-tower/overlays/eks-test/game/namespace.yaml — 9 ln

_no extracted symbols_

### infra/eks/apps/corp-tower/overlays/eks-test/web/kustomization.yaml — 20 ln

_no extracted symbols_

### infra/eks/apps/corp-tower/overlays/eks-test/web/namespace.yaml — 9 ln

_no extracted symbols_

### infra/eks/apps/corp-tower/web-base/deployment.yaml — 47 ln

_no extracted symbols_

### infra/eks/apps/corp-tower/web-base/kustomization.yaml — 4 ln

_no extracted symbols_

### infra/eks/apps/corp-tower/web-base/service.yaml — 19 ln

_no extracted symbols_

### infra/eks/apps/corp-tower/web-maintenance-base/configmap.yaml — 23 ln

_no extracted symbols_

### infra/eks/apps/corp-tower/web-maintenance-base/kustomization.yaml — 25 ln

_no extracted symbols_

### infra/eks/terraform-shared/acm.tf — 31 ln

| File:Ln | Symbol | Does |
|---|---|---|
| infra/eks/terraform-shared/acm.tf:1 | aws_acm_certificate.wildcard · resource | the persistent wildcard cert, DNS-validated, `create_before_destroy` so a renewal never leaves the ALB without one |
| infra/eks/terraform-shared/acm.tf:14 | cloudflare_record.cert_validation · resource | the DNS validation records, **`proxied = false`** — an orange-clouded validation record never validates |
| infra/eks/terraform-shared/acm.tf:27 | aws_acm_certificate_validation.wildcard · resource | blocks until ACM confirms the records, so the session stack's data lookup finds an ISSUED cert |

### infra/eks/terraform-shared/outputs.tf — 10 ln

| File:Ln | Symbol | Does |
|---|---|---|
| infra/eks/terraform-shared/outputs.tf:1 | certificate_arn · output | the validated cert ARN; **the session root re-finds it by `data "aws_acm_certificate"` rather than by remote state** |
| infra/eks/terraform-shared/outputs.tf:6 | domain_name · output | the domain the wildcard covers |

### infra/eks/terraform-shared/variables.tf — 23 ln

| File:Ln | Symbol | Does |
|---|---|---|
| infra/eks/terraform-shared/variables.tf:1 | aws_region · variable | **must be `ap-southeast-1`** — a regional ACM cert only attaches to load balancers in its own region |
| infra/eks/terraform-shared/variables.tf:7 | domain_name · variable | default `*.galaxxigames.com` |
| infra/eks/terraform-shared/variables.tf:13 | cloudflare_zone_id · variable | zone the one-time validation record is written into |
| infra/eks/terraform-shared/variables.tf:18 | cloudflare_api_token · variable | DNS-edit token, marked sensitive |

### infra/eks/terraform-shared/versions.tf — 40 ln

| File:Ln | Symbol | Does |
|---|---|---|
| infra/eks/terraform-shared/versions.tf:24 | aws · provider | AWS ~> 5.0, Terraform >= 1.10; **separate S3 state key `eks-shared/terraform.tfstate`** so the certificate survives session stacks being destroyed |
| infra/eks/terraform-shared/versions.tf:37 | cloudflare · provider | cloudflare ~> 4.0, authenticated with `var.cloudflare_api_token` |

### infra/eks/terraform/access.tf — 16 ln

| File:Ln | Symbol | Does |
|---|---|---|
| infra/eks/terraform/access.tf:1 | aws_eks_access_entry.operator · resource | standard access entry for `var.operator_principal_arn` |
| infra/eks/terraform/access.tf:7 | aws_eks_access_policy_association.operator_admin · resource | **grants that principal cluster-admin at cluster scope**, which is what makes kubectl work from an operator laptop |

### infra/eks/terraform/dashboard.tf — 62 ln

| File:Ln | Symbol | Does |
|---|---|---|
| infra/eks/terraform/dashboard.tf:5 | aws_cloudwatch_dashboard.main · resource | three widgets: ALB request count and target response time, per-target-group healthy/unhealthy counts, and node ASG CPU |

### infra/eks/terraform/data.tf — 9 ln

| File:Ln | Symbol | Does |
|---|---|---|
| infra/eks/terraform/data.tf:1 | aws_availability_zones.available · data | available AZs; `network.tf` slices the first two |
| infra/eks/terraform/data.tf:5 | aws_ecr_repository.server · data | **looks up an existing ECR repo, does not create one** — the registry outlives any single stack |

### infra/eks/terraform/eks.tf — 128 ln

| File:Ln | Symbol | Does |
|---|---|---|
| infra/eks/terraform/eks.tf:1 | aws_cloudwatch_log_group.eks_cluster · resource | **created only when `eks_cluster_log_types` is non-empty**, at retention 1 day, so an untracked no-expiry log group cannot accrue cost |
| infra/eks/terraform/eks.tf:8 | aws_eks_cluster.main · resource | the control plane at `var.kubernetes_version`, private and public endpoint access, `API_AND_CONFIG_MAP` auth with creator admin bootstrap |
| infra/eks/terraform/eks.tf:37 | aws_launch_template.node · resource | carries the node security group onto every instance the managed node group launches |
| infra/eks/terraform/eks.tf:50 | aws_eks_node_group.server · resource | managed node group on AL2023 in the private subnets, sized by `node_desired/min/max_size`, `max_unavailable = 1` on update |
| infra/eks/terraform/eks.tf:85 | aws_eks_addon_version.vpc_cni · data | resolves the newest vpc-cni matching the cluster version |
| infra/eks/terraform/eks.tf:91 | aws_eks_addon_version.kube_proxy · data | resolves the newest kube-proxy matching the cluster version |
| infra/eks/terraform/eks.tf:97 | aws_eks_addon_version.coredns · data | resolves the newest coredns matching the cluster version |
| infra/eks/terraform/eks.tf:103 | aws_eks_addon.vpc_cni · resource | pod networking addon, conflicts overwritten on create and update |
| infra/eks/terraform/eks.tf:111 | aws_eks_addon.kube_proxy · resource | service routing addon, conflicts overwritten on create and update |
| infra/eks/terraform/eks.tf:119 | aws_eks_addon.coredns · resource | cluster DNS; **depends on the node group** because CoreDNS cannot schedule before a node exists |

### infra/eks/terraform/elasticache.tf — 21 ln

| File:Ln | Symbol | Does |
|---|---|---|
| infra/eks/terraform/elasticache.tf:1 | aws_elasticache_subnet_group.redis · resource | places Redis in the private subnets |
| infra/eks/terraform/elasticache.tf:6 | aws_elasticache_replication_group.redis · resource | single-node Redis, no failover, no multi-AZ; **encryption at rest and in transit both on**, which is why the URL is `rediss://` |

### infra/eks/terraform/iam.tf — 67 ln

| File:Ln | Symbol | Does |
|---|---|---|
| infra/eks/terraform/iam.tf:1 | aws_iam_policy_document.eks_cluster_assume · data | lets eks.amazonaws.com assume the control-plane role |
| infra/eks/terraform/iam.tf:12 | aws_iam_role.eks_cluster · resource | the control plane's service role |
| infra/eks/terraform/iam.tf:17 | aws_iam_role_policy_attachment.eks_cluster_policy · resource | attaches AmazonEKSClusterPolicy |
| infra/eks/terraform/iam.tf:22 | aws_iam_policy_document.eks_node_assume · data | lets ec2.amazonaws.com assume the node role |
| infra/eks/terraform/iam.tf:33 | aws_iam_role.eks_node · resource | the worker nodes' instance role |
| infra/eks/terraform/iam.tf:38 | aws_iam_role_policy_attachment.eks_worker_node_policy · resource | attaches AmazonEKSWorkerNodePolicy |
| infra/eks/terraform/iam.tf:43 | aws_iam_role_policy_attachment.eks_cni_policy · resource | attaches AmazonEKS_CNI_Policy, required by vpc-cni |
| infra/eks/terraform/iam.tf:48 | aws_iam_role_policy_attachment.eks_ecr_pull · resource | read-only ECR pull, which is how nodes fetch the server image |
| infra/eks/terraform/iam.tf:53 | tls_certificate.eks_oidc · data | fetches the cluster OIDC issuer certificate for its thumbprint |
| infra/eks/terraform/iam.tf:57 | aws_iam_openid_connect_provider.eks · resource | registers the cluster's OIDC issuer, the prerequisite for IAM roles for service accounts |

### infra/eks/terraform/load_balancer.tf — 125 ln

| File:Ln | Symbol | Does |
|---|---|---|
| infra/eks/terraform/load_balancer.tf:1 | aws_acm_certificate.wildcard · data | looks up the issued `*.galaxxigames.com` certificate that `terraform-shared` owns |
| infra/eks/terraform/load_balancer.tf:7 | aws_lb.main · resource | internet-facing ALB across the public subnets; **`idle_timeout = 300` is what keeps game WebSockets alive** |
| infra/eks/terraform/load_balancer.tf:20 | aws_lb_target_group.game · resource | NodePort 30300; **health check expects HTTP 426** — the game server upgrades to WebSocket and correctly refuses a plain GET |
| infra/eks/terraform/load_balancer.tf:43 | aws_lb_target_group.web · resource | NodePort 30310, health check expects 200 |
| infra/eks/terraform/load_balancer.tf:66 | aws_lb_listener.https · resource | 443 with TLS 1.3 policy and the wildcard cert; default action is a fixed 404, so only the host rules below route anywhere |
| infra/eks/terraform/load_balancer.tf:84 | aws_lb_listener_rule.game · resource | priority 10, host `wstodplay.galaxxigames.com` to the game target group |
| infra/eks/terraform/load_balancer.tf:100 | aws_lb_listener_rule.web · resource | priority 20, host `todplay.galaxxigames.com` to the web target group |
| infra/eks/terraform/load_balancer.tf:116 | aws_autoscaling_attachment.game · resource | wires the node group ASG into the game target group, so scaling registers targets automatically |
| infra/eks/terraform/load_balancer.tf:121 | aws_autoscaling_attachment.web · resource | same attachment for the web target group |

### infra/eks/terraform/network.tf — 115 ln

| File:Ln | Symbol | Does |
|---|---|---|
| infra/eks/terraform/network.tf:6 | aws_vpc.main · resource | the stack's VPC at `var.vpc_cidr` (10.70.0.0/16), DNS hostnames and support both on |
| infra/eks/terraform/network.tf:16 | aws_internet_gateway.main · resource | public egress for the public subnets |
| infra/eks/terraform/network.tf:24 | aws_subnet.public · resource | one per public CIDR across two AZs; **tagged `kubernetes.io/role/elb=1`** so the ALB can place itself |
| infra/eks/terraform/network.tf:39 | aws_subnet.private · resource | node and ElastiCache subnets, no public IP; tagged `kubernetes.io/role/internal-elb=1` |
| infra/eks/terraform/network.tf:54 | aws_route_table.public · resource | default route to the internet gateway |
| infra/eks/terraform/network.tf:67 | aws_route_table_association.public · resource | binds each public subnet to the public route table |
| infra/eks/terraform/network.tf:74 | aws_eip.nat · resource | elastic IP for the NAT gateway |
| infra/eks/terraform/network.tf:84 | aws_nat_gateway.main · resource | **a single NAT in the first public subnet** — nodes reach ECR, STS and DNS through it; it is also the stack's main hourly cost |
| infra/eks/terraform/network.tf:95 | aws_route_table.private · resource | default route out via the NAT gateway |
| infra/eks/terraform/network.tf:108 | aws_route_table_association.private · resource | binds each private subnet to the private route table |

### infra/eks/terraform/outputs.tf — 45 ln

| File:Ln | Symbol | Does |
|---|---|---|
| infra/eks/terraform/outputs.tf:1 | aws_region · output | the region the stack is in |
| infra/eks/terraform/outputs.tf:6 | cluster_name · output | `corp-tower-<environment>`, the name kubectl and the deploy workflows use |
| infra/eks/terraform/outputs.tf:11 | cluster_endpoint · output | EKS API endpoint |
| infra/eks/terraform/outputs.tf:16 | alb_dns_name · output | **the two Cloudflare CNAMEs (wstodplay, todplay) are pointed here each session** — a fresh apply changes it |
| infra/eks/terraform/outputs.tf:21 | game_target_group_arn · output | target group for the server NodePort 30300 |
| infra/eks/terraform/outputs.tf:26 | web_target_group_arn · output | target group for the web NodePort 30310 |
| infra/eks/terraform/outputs.tf:31 | redis_url · output | **`rediss://` not `redis://`** — ElastiCache in-transit encryption is on, so the scheme is part of the contract |
| infra/eks/terraform/outputs.tf:36 | ecr_repository_name · output | the existing ECR repo EKS deploys pull from |
| infra/eks/terraform/outputs.tf:41 | server_eks_note · output | topology note: EKS replaces private K3s EC2 nodes, ALB host routing replaces EC2-GW Caddy, and ElastiCache over TLS replaces in-cluster Redis |

### infra/eks/terraform/security_group.tf — 106 ln

| File:Ln | Symbol | Does |
|---|---|---|
| infra/eks/terraform/security_group.tf:1 | aws_security_group.alb · resource | public ALB ingress group |
| infra/eks/terraform/security_group.tf:11 | aws_vpc_security_group_ingress_rule.alb_https · resource | 443 from `var.game_port_cidr`; the only public entry point |
| infra/eks/terraform/security_group.tf:20 | aws_vpc_security_group_egress_rule.alb_to_nodes · resource | ALB out to the node NodePort range 30300-30311 |
| infra/eks/terraform/security_group.tf:29 | aws_security_group.nodes · resource | worker node group: NodePort ingress from the ALB, outbound to ECR, STS and ElastiCache via NAT |
| infra/eks/terraform/security_group.tf:39 | aws_vpc_security_group_ingress_rule.nodes_from_alb · resource | 30300-30311 from the ALB, carrying both traffic and health checks |
| infra/eks/terraform/security_group.tf:48 | aws_vpc_security_group_egress_rule.nodes_outbound · resource | all protocols outbound, needed for ECR, STS, ElastiCache and DNS |
| infra/eks/terraform/security_group.tf:55 | aws_vpc_security_group_ingress_rule.nodes_from_nodes · resource | node-to-node any-protocol, required by CoreDNS and cross-node pod traffic under the VPC CNI |
| infra/eks/terraform/security_group.tf:62 | aws_vpc_security_group_ingress_rule.cluster_from_nodes · resource | **kubelet to control plane on 443 — without it nodes never join** |
| infra/eks/terraform/security_group.tf:71 | aws_vpc_security_group_ingress_rule.nodes_from_cluster · resource | control plane back to kubelet on 1025-65535, for logs, exec, webhooks and metrics-server |
| infra/eks/terraform/security_group.tf:80 | aws_security_group.redis · resource | ElastiCache access group |
| infra/eks/terraform/security_group.tf:90 | aws_vpc_security_group_ingress_rule.redis_from_nodes · resource | 6379 from the node group only |
| infra/eks/terraform/security_group.tf:99 | aws_vpc_security_group_egress_rule.redis_outbound · resource | Redis responses back into the VPC CIDR |

### infra/eks/terraform/variables.tf — 96 ln

| File:Ln | Symbol | Does |
|---|---|---|
| infra/eks/terraform/variables.tf:1 | aws_region · variable | default `ap-southeast-1` |
| infra/eks/terraform/variables.tf:7 | environment · variable | default `eks-lab`; feeds `local.cluster_name` as `corp-tower-<environment>` |
| infra/eks/terraform/variables.tf:13 | ecr_repository_name · variable | existing repo, default `corp-tower-server-staging` |
| infra/eks/terraform/variables.tf:19 | vpc_cidr · variable | default 10.70.0.0/16 |
| infra/eks/terraform/variables.tf:25 | public_subnet_cidrs · variable | two /24s for the internet-facing ALB |
| infra/eks/terraform/variables.tf:31 | private_subnet_cidrs · variable | two /24s for nodes and ElastiCache |
| infra/eks/terraform/variables.tf:37 | game_port_cidr · variable | who may reach the public ALB; defaults open |
| infra/eks/terraform/variables.tf:43 | kubernetes_version · variable | **must stay in AWS standard support — extended support bills the control plane at 6x**; 1.36 chosen for runway to Aug 2027 at identical cost |
| infra/eks/terraform/variables.tf:49 | eks_cluster_log_types · variable | **empty by default because CloudWatch ingestion is usage-billed**; set to ["api"] for one debugging session, which also creates a 1-day retention log group |
| infra/eks/terraform/variables.tf:55 | operator_principal_arn · variable | **an IAM role or user ARN, not an assumed-role ARN** — the access entry that gives kubectl cluster-admin |
| infra/eks/terraform/variables.tf:60 | node_instance_types · variable | default `t3.small` |
| infra/eks/terraform/variables.tf:66 | node_desired_size · variable | default 2 |
| infra/eks/terraform/variables.tf:72 | node_min_size · variable | default 1 |
| infra/eks/terraform/variables.tf:78 | node_max_size · variable | default 3 |
| infra/eks/terraform/variables.tf:84 | redis_node_type · variable | default `cache.t4g.micro` |
| infra/eks/terraform/variables.tf:90 | redis_engine_version · variable | default 7.1 |

### infra/eks/terraform/versions.tf — 37 ln

| File:Ln | Symbol | Does |
|---|---|---|
| infra/eks/terraform/versions.tf:24 | aws · provider | AWS ~> 5.0 and tls ~> 4.0, Terraform >= 1.10; **S3 backend `eks-lab/terraform.tfstate` with `use_lockfile`**, and default_tags stamping Project/Environment/Stack/ManagedBy on everything |

### scripts/art-common.sh — 68 ln

| File:Ln | Symbol | Does |
|---|---|---|
| scripts/art-common.sh:13 | die · fn | abort with a message on stderr; every failure path in the art scripts ends here |
| scripts/art-common.sh:14 | info · fn | indented progress line on stdout |
| scripts/art-common.sh:16 | load_env · fn | sources the gitignored `.env.art`, requires the four R2_* keys, maps them onto AWS_* so the aws CLI speaks R2's S3 API, and derives R2_ENDPOINT |
| scripts/art-common.sh:40 | r2 · fn | `aws s3api` bound to the R2 endpoint; all bucket calls go through it |
| scripts/art-common.sh:42 | manifest_field · fn | reads one field out of `Cor/art-manifest.json`, trying `python` then `python3` |
| scripts/art-common.sh:48 | validate_pairing · fn | **every .png must have a .import and vice versa** — Godot silently breaks on an orphan, so packaging aborts rather than shipping one |
| scripts/art-common.sh:59 | count_files · fn | recursive file count under a directory, used for the packaged-asset tally |
| scripts/art-common.sh:61 | pack_art · fn | **reproducible tarball**: sorted names, fixed mtime, uid/gid 0, `gzip -n` — without all four the archive hash changes on every run and the cache never hits |
| scripts/art-common.sh:67 | sha256_of · fn | bare sha256 of a file, no filename column |

### scripts/art-pull.sh — 67 ln

_no extracted symbols_

### scripts/art-push.sh — 83 ln

_no extracted symbols_

### scripts/backup/backup-common.sh — 176 ln

| File:Ln | Symbol | Does |
|---|---|---|
| scripts/backup/backup-common.sh:13 | die · fn | abort with a message on stderr |
| scripts/backup/backup-common.sh:14 | info · fn | indented progress line on stdout |
| scripts/backup/backup-common.sh:16 | require_cmd · fn | asserts docker, cloudflared, git, curl and jq are on PATH before anything runs |
| scripts/backup/backup-common.sh:23 | load_env · fn | sources `STATE_DIR/.env.backup`, **chmod 600s it if it is looser**, and requires the three CLOUDFLARE_* keys |
| scripts/backup/backup-common.sh:45 | load_ws_env · fn | load_env plus WS<n>_DOMAIN/WS<n>_PORT by indirect expansion, so one function serves game instances 1-3 |
| scripts/backup/backup-common.sh:58 | load_web_env · fn | load_env plus WEB<n>_DOMAIN/WEB<n>_PORT, same indirect-expansion shape as load_ws_env |
| scripts/backup/backup-common.sh:71 | set_env_value · fn | rewrites one KEY= line in `.env.backup` in place; the up scripts use it to self-record the SHA they deployed |
| scripts/backup/backup-common.sh:80 | upsert_cloudflare_cname · fn | PATCHes the CNAME if it exists, POSTs it if not; proxied=true either way, and a non-`success` response aborts |
| scripts/backup/backup-common.sh:132 | wait_for_cname · fn | **verifies via the Cloudflare API, never `dig`** — a proxied record exposes no public CNAME, so dig would report a working cutover as failed |
| scripts/backup/backup-common.sh:155 | start_cloudflared_if_needed · fn | starts the shared user-level cloudflared service; idempotent by design |
| scripts/backup/backup-common.sh:165 | stop_cloudflared_if_idle · fn | **only stops the tunnel when none of the six containers is running** — all six share one cloudflared, so an unguarded stop cuts the others off |

### scripts/backup/backup-server-down.sh — 26 ln

_no extracted symbols_

### scripts/backup/backup-server-status.sh — 45 ln

_no extracted symbols_

### scripts/backup/backup-server-up.sh — 74 ln

_no extracted symbols_

### scripts/backup/backup-web-down.sh — 29 ln

_no extracted symbols_

### scripts/backup/backup-web-status.sh — 67 ln

_no extracted symbols_

### scripts/backup/backup-web-up.sh — 71 ln

_no extracted symbols_

### scripts/build-file-map.mjs — 344 ln

| File:Ln | Symbol | Does |
|---|---|---|
| scripts/build-file-map.mjs:23 | CLIENT · const | the Godot project root prefix, used by both the ui area roots and the ignore list |
| scripts/build-file-map.mjs:27 | AREAS · const | **the single source of truth for what counts as first-party source**; a file is claimed by the FIRST area whose root contains it, which is what makes exactly-one-map enforceable |
| scripts/build-file-map.mjs:54 | IGNORE_DIR · const | never-walked directories: addons, node_modules, .godot, .terraform, .git |
| scripts/build-file-map.mjs:55 | IGNORE_PATH · const | path-level excludes — client art, and the two separately-deployed Workers sites |
| scripts/build-file-map.mjs:64 | COVERAGE_EXEMPT · const | tests are routed by testing.md, not by symbol map; their useful unit is the case name, which greps fine from source |
| scripts/build-file-map.mjs:69 | norm · const | backslash to forward slash, so generated paths are identical on Windows and CI |
| scripts/build-file-map.mjs:71 | walk · fn | recursive directory walk, sorted for deterministic output |
| scripts/build-file-map.mjs:87 | firstPartyFiles · fn | every first-party file tagged with its owning area; **imported by validate-docs.mjs** rather than duplicated there |
| scripts/build-file-map.mjs:109 | isExempt · const | true for test files, which are excluded from map coverage |
| scripts/build-file-map.mjs:117 | JS_KEYWORD · const | words that look like a method signature but are control flow; keeps `if (...) {` out of the map |
| scripts/build-file-map.mjs:122 | symbolsJs · fn | class, function, exports., top-level const and class-body method; **`const X = require(...)` is skipped** — an import cannot answer which file does X |
| scripts/build-file-map.mjs:152 | symbolsGd · fn | class_name, signal, const, static var, `@export` var and func; **plain and `@onready` var are skipped** as node plumbing nothing routes to |
| scripts/build-file-map.mjs:181 | symbolsTf · fn | resource, module, variable, output, data and provider blocks, named `type.name` |
| scripts/build-file-map.mjs:190 | symbolsYml · fn | top-level keys plus job names under `jobs:`, which is the routable unit of a workflow |
| scripts/build-file-map.mjs:206 | symbolsSh · fn | shell function definitions |
| scripts/build-file-map.mjs:215 | extract · fn | dispatches to the per-language extractor by extension and returns line count with symbols |
| scripts/build-file-map.mjs:231 | readAuthored · fn | **carry-forward**: parses the existing map so hand-written `Does` prose survives regeneration, keyed on `path#symbol` and never on line number; accepts both the old bare-number and the current `path:line` first cell |
| scripts/build-file-map.mjs:254 | render · fn | emits the markdown; each row carries its own `path:line` so one grep hit needs no follow-up lookup |
| scripts/build-file-map.mjs:294 | build · fn | walks every area, reads each file, and renders against the `Does` prose already in the map |
| scripts/build-file-map.mjs:312 | main · fn | `--check` exits 1 on a stale map without writing; `--quiet` prints the summary line only |

### scripts/docs-scope.mjs — 218 ln

| File:Ln | Symbol | Does |
|---|---|---|
| scripts/docs-scope.mjs:17 | argv · const | raw CLI args; the tool takes explicit paths, not a diff |
| scripts/docs-scope.mjs:18 | FROM_GIT · const | **opt-in only** — scoping from the working tree sweeps in every concurrent agent's in-flight change and documents work this task never did |
| scripts/docs-scope.mjs:22 | rangeAt · const | index of `--range` in argv |
| scripts/docs-scope.mjs:23 | RANGE · const | git range to diff against; defaults to HEAD, set to `<sha>^..<sha>` when the task is already committed |
| scripts/docs-scope.mjs:25 | ROOT · const | repo root, overridable via DOCS_SCOPE_ROOT for testing |
| scripts/docs-scope.mjs:26 | CTX · const | the docs/context directory this tool routes into |
| scripts/docs-scope.mjs:27 | CLIENT · const | Godot project prefix, stripped to form a second search needle per path |
| scripts/docs-scope.mjs:28 | git · const | execFileSync git wrapper with stderr discarded |
| scripts/docs-scope.mjs:33 | IGNORE · const | binary, generated and out-of-KB paths — including `docs/context/` itself, plan/, task/ and reference/ |
| scripts/docs-scope.mjs:46 | ROUTES · const | **path to owning doc, first match wins**, with a read strategy per rule: `full` for files encoding contracts, `wide` for -U10, `hunk` for -U2 |
| scripts/docs-scope.mjs:76 | MAP_OWNER · const | which map a path's line-number churn invalidates, so the run says what to regenerate |
| scripts/docs-scope.mjs:83 | given · const | the explicit paths passed by the caller, minus the --range value |
| scripts/docs-scope.mjs:93 | paths · const | deduped candidate set before the ignore map is applied |
| scripts/docs-scope.mjs:105 | dropped · const | paths removed by IGNORE, reported as a count so a wrong ignore is visible |
| scripts/docs-scope.mjs:106 | scoped · const | the surviving doc-relevant paths |
| scripts/docs-scope.mjs:121 | churn · const | per-path `+adds/-dels`, or `new`, `deleted`, or **`no diff vs RANGE`** — the last means the task is already committed and needs --range |
| scripts/docs-scope.mjs:128 | tracked · const | git ls-files probe distinguishing an untracked new file from an unchanged one |
| scripts/docs-scope.mjs:138 | byDoc · const | owning doc to the paths that route into it, the report's primary grouping |
| scripts/docs-scope.mjs:139 | unmapped · const | paths no ROUTES rule matched; the fix is a new rule, not a hand-route |
| scripts/docs-scope.mjs:153 | needles · const | basename, full path, and client-relative path — the three forms a doc might use to name a file |
| scripts/docs-scope.mjs:154 | targets · fn | **the falsification set**: only sections mentioning a changed file can have been made false, so step 4 reads those line ranges instead of the whole doc |
| scripts/docs-scope.mjs:175 | outline · fn | `##` headings with line numbers, so a new entry can be placed without opening the doc |
| scripts/docs-scope.mjs:187 | order · const | docs sorted by how many paths route into them, heaviest first |
| scripts/docs-scope.mjs:208 | maps · const | the distinct map files needing regeneration after this task |

### scripts/strip-comments.mjs — 293 ln — enforces the no-comments-in-source rule

| File:Ln | Symbol | Does |
|---|---|---|
| scripts/strip-comments.mjs:26 | STRIP_AREAS · const | backend + ui only; **`scripts/` and `.github/` are out of scope** and keep their comments |
| scripts/strip-comments.mjs:29 | SAFETY · const | `SAFETY EXCEPTION` lines are kept verbatim wherever they appear |
| scripts/strip-comments.mjs:30 | GD_REGION · const | `#region`/`#endregion` are folding markers, not comments |
| scripts/strip-comments.mjs:36 | REGEX_AFTER_WORD · const | keywords after which `/` opens a regex rather than dividing |
| scripts/strip-comments.mjs:40 | WORD_CHAR · const | identifier-character test for the regex/division decision |
| scripts/strip-comments.mjs:42 | regexAllowed · fn | **the `/` ambiguity**: after a value it divides, after an operator or keyword it opens a regex |
| scripts/strip-comments.mjs:54 | scanJs · fn | char-level scanner: string, template (with `${}` nesting), regex and block-comment states |
| scripts/strip-comments.mjs:123 | scanGd · fn | same for GDScript: `#` outside single, double and both triple-quoted string forms |
| scripts/strip-comments.mjs:159 | strip · fn | emits per line; drops comment-only lines, rtrims trailing ones, **preserves per-file CRLF/LF** |
| scripts/strip-comments.mjs:208 | keyBag · const | counts `identifier:` keys on RAW text, so gate 3 does not trust the scanner |
| scripts/strip-comments.mjs:214 | verify · fn | **three gates, any failure aborts the whole run**: no code line dropped · code content byte-identical · no config key lost |
| scripts/strip-comments.mjs:246 | main · fn | dry-run and write modes; gates run on every file before a byte is written |

### scripts/validate-docs.mjs — 435 ln

| File:Ln | Symbol | Does |
|---|---|---|
| scripts/validate-docs.mjs:25 | argv · const | raw CLI args |
| scripts/validate-docs.mjs:26 | QUIET · const | suppresses the per-doc table on a passing run; a failing run always prints in full |
| scripts/validate-docs.mjs:27 | ROOT · const | repo root being validated |
| scripts/validate-docs.mjs:28 | CTX · const | the docs/context directory |
| scripts/validate-docs.mjs:29 | MAP · const | the docs/context/map directory, budgeted and reported separately from prose |
| scripts/validate-docs.mjs:30 | errors · const | hard failures; a non-empty list exits 1 |
| scripts/validate-docs.mjs:31 | warnings · const | soft findings that never fail the run |
| scripts/validate-docs.mjs:33 | tok · const | **the token estimate is `bytes/4`** — deliberately no tokenizer dependency, deliberately conservative |
| scripts/validate-docs.mjs:37 | DEFAULT_BUDGET · const | 3000 tokens for any doc without an explicit entry |
| scripts/validate-docs.mjs:45 | BUDGETS · const | per-doc token ceilings, each set just above measured size so the next unjustified addition fails loudly |
| scripts/validate-docs.mjs:67 | MAP_BUDGET · const | **a bloat alarm, not a load budget** — a map is grepped, never loaded, so its cost is per-hit and does not scale with file size |
| scripts/validate-docs.mjs:70 | PROSE_TOTAL_BUDGET · const | whole-KB prose ceiling; maps are excluded from it and reported on their own |
| scripts/validate-docs.mjs:71 | MAX_LINE_CHARS · const | **300, and the reason the budget cannot be gamed** — without it a doc meets any token budget by folding an essay into one table cell |
| scripts/validate-docs.mjs:72 | NET_GROWTH_WARN · const | net added lines per doc per run above which the run asks whether you are transcribing rather than documenting |
| scripts/validate-docs.mjs:79 | BANNED · const | constructions that turn a description of the system into a story about it; **`Rejected:` is on the list** because it was the loophole the others leaked through |
| scripts/validate-docs.mjs:80 | BANNED_EXEMPT · const | deliberately matches nothing — the banned list has no exemption |
| scripts/validate-docs.mjs:83 | STATUS · const | unresolved-commitment markers surfaced at the end of every run |
| scripts/validate-docs.mjs:86 | slug · fn | GitHub heading to anchor slug, used for both anchor generation and link checking |
| scripts/validate-docs.mjs:92 | files · const | the prose docs directly under docs/context |
| scripts/validate-docs.mjs:93 | mapFiles · const | the generated map files |
| scripts/validate-docs.mjs:94 | read · const | reads a prose doc |
| scripts/validate-docs.mjs:95 | readMap · const | reads a map file |
| scripts/validate-docs.mjs:96 | all · const | prose and map docs in one list, map entries prefixed `map/` |
| scripts/validate-docs.mjs:100 | anchors · const | per-doc anchor sets, with GitHub's `-2` suffix rule for duplicate headings |
| scripts/validate-docs.mjs:117 | referenced · const | docs linked from somewhere; the complement is the orphan warning |
| scripts/validate-docs.mjs:156 | source · const | first-party files from build-file-map.mjs, minus tests — the map-coverage denominator |
| scripts/validate-docs.mjs:157 | mapText · const | map filename to contents, used by both coverage directions |
| scripts/validate-docs.mjs:181 | FILEISH · const | matches anything shaped like a repo filename inside prose or a comment |
| scripts/validate-docs.mjs:184 | CITATION_ALLOW · const | names that legitimately resolve outside the walked tree — `Node.js` is a runtime that any regex reads as a file |
| scripts/validate-docs.mjs:191 | CITATION_SKIP_FILES · const | the two tools whose job is quoting names that do not resolve; without this the checker reports its own error strings as drift |
| scripts/validate-docs.mjs:197 | GLOBBY_SPAN · const | a backticked span holding a glob or brace expansion names a set, so no single basename in it is expected to resolve |
| scripts/validate-docs.mjs:198 | maskGlobs · const | blanks those spans before scanning, rather than allow-listing the fragments, which would silence the real name too |
| scripts/validate-docs.mjs:201 | CITATION_SKIP_SECTIONS · const | `## Aliases` exists to name retired things, so the dead half not resolving is the entry's purpose |
| scripts/validate-docs.mjs:203 | CITES_OK · const | per-line escape for one deliberate reference to something gone |
| scripts/validate-docs.mjs:204 | basenames · const | every filename in the tree, the set citations are checked against |
| scripts/validate-docs.mjs:216 | checkCitations · fn | **a doc or source comment naming a repo file must name one that exists**; skips fenced blocks and alias sections |
| scripts/validate-docs.mjs:232 | COMMENT · const | extracts the text of a `//` or `#` comment for citation checking |
| scripts/validate-docs.mjs:252 | WORD_NUM · const | spelled-out numbers one through twelve, so a counted claim written in words is still checkable |
| scripts/validate-docs.mjs:253 | treeFiles · const | source files under 400 KB that counted claims are verified against; **markdown is excluded** or writing about a count would inflate it |
| scripts/validate-docs.mjs:272 | countToken · fn | case-insensitive occurrence count of a token across the tree |
| scripts/validate-docs.mjs:312 | pkgPath · const | the server package.json, read for dependency-drift warnings |
| scripts/validate-docs.mjs:328 | growth · const | net line change per doc vs HEAD, which decides hard error versus warning |
| scripts/validate-docs.mjs:329 | addedLines · const | the exact lines this run added, so a banned phrase is a hard error only if newly written |
| scripts/validate-docs.mjs:331 | git · const | execFileSync git wrapper; failure leaves gitOk false and downgrades everything to warnings |
| scripts/validate-docs.mjs:354 | grew · const | true if the doc gained net lines this run |
| scripts/validate-docs.mjs:355 | flag · const | **growth is blocked, compaction never is** — the same violation is an error in a doc that grew and a warning in one that shrank, so an over-budget doc can always be edited back down |
| scripts/validate-docs.mjs:356 | isNew · const | true if this exact line was added in this run |
| scripts/validate-docs.mjs:359 | statusMarkers · const | unresolved markers to keep, resolve or delete, listed at the end of the run |
| scripts/validate-docs.mjs:360 | counts · const | per-doc token, budget and line count rows for the report |
| scripts/validate-docs.mjs:395 | proseTotal · const | summed prose tokens, checked against PROSE_TOTAL_BUDGET |
| scripts/validate-docs.mjs:396 | mapTotal · const | summed map tokens, reported but not part of the prose total |
| scripts/validate-docs.mjs:413 | terse · const | true only on a quiet passing run |

### scripts/write-endpoint-config.sh — 36 ln

_no extracted symbols_

---

81 files · 338 symbols · 0 awaiting a `Does` line.
