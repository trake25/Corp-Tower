src/Client/App/corp-tower/export_presets.cfg
.obsidian/

# Terraform
infra/bootstrap/.terraform/
infra/bootstrap/*.tfstate
infra/bootstrap/*.tfstate.*
infra/k3s/terraform/.terraform/
infra/k3s/terraform/*.tfstate
infra/k3s/terraform/*.tfstate.*
infra/k3s/terraform/k3s-lab.tfvars
infra/eks/terraform/.terraform/
infra/eks/terraform/*.tfstate
infra/eks/terraform/*.tfstate.*
infra/eks/terraform/eks-lab.tfvars
# Corp Tower GDD
*.docx
*.docx.bak
*.exe
/.cursor
.claude/
CLAUDE.md
plan/
task/
reference/
*.x86_64

# Private art asset pipeline — R2 credentials for local dev.
# The assets themselves are ignored in src/Client/App/corp-tower/.gitignore
.env.art

# Physical backup machine credentials — see scripts/backup/.env.backup.example.
# The filled-in copy belongs outside the repo checkout; ignored here only as
# a safety net if one ever ends up inside it.
.env.backup
scripts/backup/.env.backup

# CI-downloaded web build artifact staged as the docker/web Docker build context
docker/web/web/

# Portfolio site (Astro)
site/
site/node_modules/
site/dist/
site/.astro/
site/.wrangler/
