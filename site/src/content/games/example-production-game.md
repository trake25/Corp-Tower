---
# ---------------------------------------------------------------------------
# TEMPLATE — copy this file, rename it, fill it in.
#
# While `hidden: true` it renders nowhere: not in 00 Playables, not in the
# In Production vignette, not in any count. Set it to false to publish.
# Delete this file once you have a real game in here.
# ---------------------------------------------------------------------------
title: "Game title"
status: "production"
order: 1
hidden: true
tagline: "One short line — genre, players, platforms."
blurb: "Two or three sentences on what the game is and who it is for. This is what shows when someone opens the tile, so it should stand on its own without the links underneath it."
# Every link here also appears in section 00 Playables, which is the shortcut
# for a visitor who just wants to play. Keep them to real play destinations.
links:
  - label: "itch.io"
    href: "https://itch.io/"
  - label: "Google Play"
    href: "https://play.google.com/"
# Optional. Drop the whole `repo:` block if this game has no public source.
# `disclaimer` is only for when the repository name differs from the game's.
# repo:
#   label: "Source on GitHub"
#   href: "https://github.com/trake25/example"
#   disclaimer: ""
# Optional trailer or gameplay clip. Drop the block and nothing renders.
# video:
#   src: "https://assets.example.com/trailer.mp4"
#   poster: "https://assets.example.com/trailer-poster.jpg"
#   caption: "Ninety seconds of a full round."
---

Anything below the frontmatter is optional prose, shown under the links when the
tile is open. Delete it if the blurb already says enough.
