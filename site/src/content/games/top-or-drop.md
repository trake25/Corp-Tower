---
title: "Top or Drop"
status: "development"
order: 1
tagline: "Real-time multiplayer · three players · browser and Android"
blurb: "Three players cooperate to build one tower and still compete to outscore each other. A Godot client against a server-authoritative Node.js and Redis backend, playable in the browser today with an Android build in an internal testing track. Bots fill empty seats, so a single visitor still gets a full round."
links:
  - label: "Play the demo"
    href: "https://toddemo.galaxxigames.com"
repo:
  label: "Source on GitHub"
  href: "https://github.com/trake25/Corp-Tower"
  disclaimer: "The repository still carries the project's working name, Corp Tower. Every evidence link below points at real files under that name, so it is kept rather than renamed."
buildingTheGame: true
# Paths are relative to `profile.mediaBase` — the R2 public host is set once
# there, not repeated per file.
video:
  src: "clips/top-or-drop-demo-win.mp4"
  poster: "clips/top-or-drop-demo-win.jpg"
  caption: "A full round in the browser demo, played through to a win."
---

The demo runs from the development environment rather than production, which
keeps the cost of an always-available demo near zero and means its latency is
higher than a production session's. It is still in development — things will
change.
