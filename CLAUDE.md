@AGENTS.md

# Claude Code adapter

Project skills are generated under `.claude/skills/` from the canonical
`.agents/skills/` tree. Edit only the canonical tree and run
`node scripts/sync-agent-skills.mjs`.
