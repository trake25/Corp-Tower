@AGENTS.md

# Claude Code adapter

`AGENTS.md` is the universal execution policy. The approved Phase 2 plan supplies task behavior, retrieval inputs, changes, invariants, verification, and non-default overrides.

This adapter adds no domain or process policy.

Enable versioned Git hooks once per clone with `node scripts/install-git-hooks.mjs`.
