# Concept Map — build

DRAFT GENERATED OUTPUT. The repository generator should validate every source target,
resolve stable anchors to current line numbers, and emit bounded source-read ranges.

## build.android.aab-validation

Owner: `build.md` → **AAB validation**

| Source seed | Status |
|---|---|
| `.github/workflows/Android-Deploy-wstodplay.yml#@file` | coarse `@file` seed — refine before activation |

## build.android.pipeline

Owner: `build.md` → **Android pipeline**

| Source seed | Status |
|---|---|
| `.github/workflows/Android-Deploy-wstodplay.yml#@file` | coarse `@file` seed — refine before activation |

Adjacent concepts: `testing.release.gates`

## build.android.startup-splash

Owner: `build.md` → **Startup splash**

| Source seed | Status |
|---|---|
| `src/Client/App/corp-tower/project.godot#@file` | coarse `@file` seed — refine before activation |

Adjacent concepts: `ui.startup.splash`, `testing.client.rendered`

## build.android.version-code

Owner: `build.md` → **Play version code**

| Source seed | Status |
|---|---|
| `.github/workflows/Android-Deploy-wstodplay.yml#@file` | coarse `@file` seed — refine before activation |

## build.art.private-bundle

Owner: `build.md` → **Private art bundle**

| Source seed | Status |
|---|---|
| `scripts/art-publish.mjs#@file` | coarse `@file` seed — refine before activation |

## build.auth.native-providers

Owner: `build.md` → **Native provider build inputs**

| Source seed | Status |
|---|---|
| `.github/workflows/Android-Deploy-wstodplay.yml#@file` | coarse `@file` seed — refine before activation |

## build.endpoint-auth.injection

Owner: `build.md` → **Endpoint and auth injection**

| Source seed | Status |
|---|---|
| `scripts/write-endpoint-config.sh#@file` | coarse `@file` seed — refine before activation |

Adjacent concepts: `ui.auth.presentation`, `network.session.identity`

## build.godot.asset-import

Owner: `build.md` → **Godot asset import**

| Source seed | Status |
|---|---|
| `src/Client/App/corp-tower/project.godot#@file` | coarse `@file` seed — refine before activation |

## build.server.image

Owner: `build.md` → **Server image**

| Source seed | Status |
|---|---|
| `src/Server/Dockerfile#@file` | coarse `@file` seed — refine before activation |

Adjacent concepts: `deploy.eks.topology`, `deploy.backup.topology`

