apiVersion: v1
kind: Service
metadata:
  name: corp-tower-web
  labels:
    app.kubernetes.io/name: corp-tower-web
    app.kubernetes.io/part-of: corp-tower
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: corp-tower-web
    app.kubernetes.io/part-of: corp-tower
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30310
