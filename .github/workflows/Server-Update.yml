# DEPRECATED — kept for reference only.
# Use .github/workflows/Server-Staging-Deploy.yml (Docker + ECR + staging EC2).
# Legacy production EC2 deploy (git pull + PM2) is preserved below but will not run.

name: Server Update (DEPRECATED)

on:
  workflow_dispatch:

concurrency:
  group: server-update-deprecated
  cancel-in-progress: true

jobs:
  deprecated-notice:
    name: Workflow Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain deprecation
        run: |
          echo "::error::Server-Update.yml is deprecated and disabled."
          echo "Use Server-Staging-Deploy.yml instead."
          echo "See docs/deploy-staging.md"
          exit 1

# --- Original workflow (reference only; not executed) ---
# name: Server Update
# on:
#   push:
#     branches: [main, master]
#   workflow_dispatch:
# jobs:
#   test-server: ...
#   deploy-server: SSH git pull + npm + pm2 to secrets.EC2_HOST
