name: K3s Cleanup Web-todtest

on:
  workflow_dispatch:
    inputs:
      confirm_cleanup:
        description: "Type CLEANUP_TODTEST to remove the todtest web server (Deployment+Service) from corp-tower-test. The namespace and any game server there are left untouched."
        required: true
        type: string

jobs:
  guard:
    name: Check Cleanup Confirmation
    runs-on: ubuntu-24.04
    steps:
      - name: Verify confirmation
        env:
          CONFIRM: ${{ inputs.confirm_cleanup }}
        run: |
          set -euo pipefail
          if [ "$CONFIRM" != "CLEANUP_TODTEST" ]; then
            echo "::error::confirm_cleanup must be exactly CLEANUP_TODTEST."
            exit 1
          fi

  cleanup:
    name: Cleanup todtest
    needs: guard
    if: needs.guard.result == 'success'
    uses: ./.github/workflows/K3s-Cleanup-Web-Server.yml
    with:
      target: todtest
    secrets: inherit
